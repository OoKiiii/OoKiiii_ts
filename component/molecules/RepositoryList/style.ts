import { styled } from '@stitches/react';

export const RepositoryListStyle = styled('div', {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
});

export const RepositoryListContent = styled('div', {
    flex: '0 1 1',
});
