import { styled } from '@stitches/react';

export const NavigationWrap = styled('div', {
    width: '100%',
    maxWidth: '350px',
    height: '100%',
    minHeight: '100vh',
    borderRight: '1px solid #ececec',
});
